on:
  push:
    - dev

jobs:
  test-job:
    runs-on: ubuntu-latestßßß

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install packages
        run: npm install

      - name: Test the code
        run: npm test
